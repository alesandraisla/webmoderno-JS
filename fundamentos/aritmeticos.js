const [a, b, c, d] = [3,5,2,15];

const soma = a + b + c + d;
const subtracao = d - b;
const multiplicacao = a * b;
const divisao = d / a;
const modulo = a % 2; //numero par é divisel por 2 então será 0, caso contrário será módulo impar 

console.log(soma, subtracao, multiplicacao, divisao, modulo);

